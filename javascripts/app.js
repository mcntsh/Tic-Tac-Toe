(function(global) {

    'use strict';

    var // Fleshing out the backbone of our app architecture
        App        = global.App || {};
        App.Util   = App.Util   || {};
        App.Module = App.Module || {};
        App.View   = App.View   || {};

    global.App = App; // Expose our object to the global scope

})(this);
